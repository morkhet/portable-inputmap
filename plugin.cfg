[plugin]

name="portable-inputmap"
description="plugin for easy import and export of the inputmap. uses the config file directly in order to have an editable inputmap outside the runtime"
author="morkhet"
version=""
script="portable-inputmap.gd"
