[plugin]

name="portable-inputmap"
description="Plugin for easy import and export of inputmap(s). This plugin uses the config files directly for persistant changes (runtime-independed). text-files and binary files supported."
author="morkhet"
version="1.1"
script="portable-inputmap.gd"
